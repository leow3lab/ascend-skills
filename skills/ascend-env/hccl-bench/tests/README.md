# hccl-bench 测试框架

**目的**：遵循 RULE.md 规则 9（行为回归测试），确保 Skill 的逻辑、数据、行为在版本迭代中保持一致。

---

## 测试目录结构

```
tests/
├── README.md                     # 本文档
├── test_scripts.sh               # 脚本可执行性测试
└── check_env.sh                # 环境对齐检查
```

---

## 注意事项

**当前状态**：`hccl-bench` 脚本尚未实现，`scripts/` 目录下仅有 `TODO.md`。

**测试框架现状**：
- `test_scripts.sh`：当前检查 TODO.md 文件存在性和内容
- `check_env.sh`：可正常执行，检查 HCCL 测试所需的组件
- `README.md`：待脚本实现后补充完整测试用例

---

## 测试类型

### 1. 脚本执行测试 (`test_scripts.sh`)

**当前测试内容**（占位）：
- [x] TODO.md 文件存在性
- [x] TODO.md 非空检查

**待实现后测试**：
- [ ] 脚本文件存在性
- [ ] 脚本可执行权限
- [ ] `--help` 参数支持
- [ ] 无硬编码敏感值
- [ ] shebang 规范性

**执行方式**：`bash tests/test_scripts.sh`

---

### 2. 环境对齐检查 (`check_env.sh`)

**检查项**：
- [x] NPU 驱动（必需）
- [x] CANN（必需）
- [x] NPU 设备数量
- [x] mpirun（可选，多节点需要）
- [x] hostfile（可选，多节点配置需要）

**执行方式**：`bash tests/check_env.sh`

---

## 测试覆盖率

| 测试类型 | 覆盖率 | 状态 |
|---------|---------|------|
| 脚本可执行性 | N/A | ⏳ 待脚本实现 |
| 环境对齐检查 | 100% | ✅ 完成 |

---

## 后续工作

**待脚本实现后需要更新：**

1. `test_scripts.sh`
   - 添加实际脚本列表（如 `execute_hccl_test.sh`）
   - 实现完整的 6 维度测试

2. `check_env.sh`
   - 验证实际测试脚本所需的额外依赖
   - 添加测试脚本执行验证

3. `README.md`
   - 补充完整的测试用例说明
   - 添加单节点和多节点测试的验证流程

---

**维护者**：Ascend Team
**最后更新**：2026-03-12
**状态**：框架已创建，待脚本实现后补充用例